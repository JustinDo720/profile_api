from django.contrib import admin
from .models import Profile, Student, Achievement

# Register your models here.
admin.site.register(Profile)
admin.site.register(Student)
admin.site.register(Achievement)
